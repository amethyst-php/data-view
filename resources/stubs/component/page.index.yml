extends: _.page.index
options:
  data: {{ name }}
  fields:
  {% for attribute in attributes %}
    - {{ attribute.name }}
  {% endfor %}actions:
    global:
      - user.resource.create
    resource:
      - user.resource.update
      - _.resource.delete